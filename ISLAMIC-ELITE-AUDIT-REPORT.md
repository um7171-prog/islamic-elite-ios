# تقرير تدقيق شامل — تطبيق النخبة الإسلامية

**النطاق:** فحص قراءة فقط لكامل المشروع (`qibla-divine-compass-main`) — لم يُعدَّل أو يُحذف أو يُنشأ أي ملف كود خلال هذا التدقيق، ولم تُثبَّت أي حزمة.
**التاريخ:** 2026-09-13
**المصدر:** 5 وكلاء فحص متخصصين (بنية عامة/صفحات، مركز الخدمات ومصادر البيانات، خطة حذف TikTok/YouTube، QR والماسح الضوئي، التصميم البصري وRTL) + فحوصات مباشرة (`tsc`, `eslint`, `vitest`, `build`).

> ⚠️ **هذا تقرير تدقيق فقط. لم تُنفَّذ أي إصلاحات أو تعديلات أو عمليات حذف. لا شيء يُنفَّذ من هذه الخطة قبل مراجعتها واعتمادها صراحة.**

---

## الخلاصة السريعة

الأساس الهندسي للتطبيق سليم: **0 أخطاء TypeScript**، **5/5 اختبارات ناجحة**، **بناء ناجح بلا أخطاء** — لا حاجة لإعادة كتابة جذرية. المشاكل الحقيقية محصورة في: تكرار أدوات وكود ميت متراكم (~1900 سطر)، ماسح QR/المستندات يعمل لكن على أساس تقني هش (Web API بدل Capacitor + تسريب كاميرا فعلي)، تصميم غير متّسق أكثر منه "سيّئ" (توكنات ألوان جيدة لكن غير مُستخدَمة، أربعة أساليب متضاربة لسهم الرجوع في RTL)، وبيانات تقويم دراسي مكتوبة يدويًا تنتهي صلاحيتها الفعلية في يونيو 2027.

---

## 1. الملاحظات التصميمية والتنظيمية

### 1.1 نظام الألوان والطباعة
- التوكنات المركزية (`index.css` عبر متغيرات HSL، و`tailwind.config.ts`) مصمَّمة جيدًا بثيمين (ليلي/نهاري)، لكن الاستخدام الفعلي في المكوّنات يتجاوزها:
  - تدرّجات الذهبي/الزمردي/الأزرق مُعرَّفة يدويًا **بنفس القيم** في ملفين JS منفصلين على الأقل (`ServicesHub.tsx`, `QuickServices.tsx`) رغم وجود `--gradient-gold` و`--gradient-emerald` جاهزة في `index.css`.
  - ألوان مباشرة hardcoded إضافية: `fill-yellow-400`, `bg-emerald-500` بدل توكنات الثيم.
  - لا سلّم طباعة موحّد: عشرات القيم الحرّة المتفرقة (`text-[10px]` حتى `text-[13px]`) عبر عشرات الصفحات بدل سلّم ثابت (caption/body/title/h1). كل صفحة تقريبًا "تخترع" حجم H1 الخاص بها.
  - حاويات الأيقونات تتراوح من `h-7` إلى `h-20` بلا نظام واضح، أحيانًا بين بطاقتين متجاورتين في نفس الشاشة.
  - نصف قطر الحواف (`rounded-lg`/`rounded-xl`/`rounded-2xl`) يُستخدم بالتبادل لعناصر من نفس الفئة دون قاعدة واضحة.

### 1.2 ازدحام مركز الخدمات
33 أداة في شبكة عمودين ثابتة (`grid-cols-2`) بحجم بطاقة صغير جدًا (حشو `p-3`, أيقونة `h-11`, عنوان `text-[12.5px]`). هذا القسم لا يظهر بمفرده بل يُكدَّس ضمن تبويب واحد بعد: 10 اختصارات سريعة (`QuickServices`) + التقويم الشخصي (`EventsCalendar`) + 33 بطاقة أداة + قسم أدوات إضافي (`EliteTools`) — كلها بتمرير عمودي واحد متواصل.

### 1.3 سهم الرجوع في RTL/LTR — أربعة أساليب متضاربة
التطبيق يبدّل `dir` ديناميكيًا فعليًا عند تبديل اللغة (وليس RTL ثابت)، ما يجعل هذا التضارب حقيقيًا وليس نظريًا:

| الأسلوب | أين يُستخدم |
|---|---|
| تبديل المكوّن حسب الاتجاه (`ArrowRight`/`ArrowLeft`) | `StaticPageShell.tsx`, `AI.tsx`, `TikTok.tsx`, `AiToolPage.tsx` |
| تدوير 180° في RTL | `Settings.tsx`, `QiblaDialog.tsx` |
| منطق معكوس (تدوير *في LTR* بدل RTL) | `SitemapPage.tsx` — **يحتاج تحققًا يدويًا لأنه معكوس عن البقية** |
| لا معالجة اتجاه إطلاقًا | `MushafBars.tsx` — **خطأ اتجاه مؤكد عند التبديل للإنجليزية** |

### 1.4 حالات التحميل/الفراغ/الخطأ غير متسقة
مكوّن `Skeleton` جاهز في `src/components/ui/skeleton.tsx` لكنه **غير مستخدم فعليًا** في أي صفحة تجلب بيانات. كل مكوّن يطبّق نمطه الخاص: نص "جاري التحميل…" + `Loader2` في مكان، أيقونة دوّارة تستبدل أيقونة أخرى في مكان ثانٍ، ولا شيء صريح في أماكن أخرى. لا توجد حالة "لا نتائج" مصمَّمة بعناية (رسم/أيقونة + نص مساعد) — فقط سطر نصي رمادي صغير متكرر.

### 1.5 مشاكل بسيطة إضافية
- ترويسة الصفحة الرئيسية (`Index.tsx`) تستخدم حشوًا فيزيائيًا غير متماثل (`pl-3 pr-2`) بدل خصائص منطقية — يختل التوازن البصري عند التبديل من عربي لإنجليزي.
- عدم استخدام `React.lazy`/`Suspense` لأي صفحة أو حوار — كل الحوارات الثقيلة (ماسح المستندات، أدوات AI) تُحمَّل ضمن الحزمة الأولى.
- ارتفاع ثابت `h-[420px]` لإطار خريطة الطقس (`WeatherDialog.tsx`) قد يبدو كبيرًا على شاشة iPhone SE القصيرة.

### 1.6 إيجابيات مؤكدة (لا تحتاج تغييرًا)
- `100dvh` بدل `100vh` مستخدَم باتساق في كل الهيكل الأساسي (يتفادى مشكلة شريط عنوان Safari).
- `env(safe-area-inset-*)` مطبَّق بعناية على مستويين: الشريط السفلي نفسه ومنطقة التمرير عبر متغيّر `--bottom-nav-h`.
- `<html lang="ar" dir="rtl">` مضبوط مركزيًا وديناميكيًا عبر `LocaleContext`.
- رسائل أخطاء Supabase مترجَمة فعليًا للعربية عبر دوال `translateError()` مخصصة.
- مكتبة الأيقونات (`lucide-react`) موحّدة كمصدر واحد بأحجام وزن بصري متسقة غالبًا (`h-4 w-4` الأكثر استخدامًا).

---

## 2. الأخطاء والمشاكل الوظيفية

### حرجة
| المشكلة | الملف | الوصف |
|---|---|---|
| تسريب مصدر الكاميرا (Race Condition) | `QRScannerDialog.tsx`, `DocumentScannerDialog.tsx` | `start()` غير متزامن (ينتظر قرار إذن المستخدم) بينما `stop()` في تنظيف `useEffect` ينفَّذ فورًا عند إغلاق الحوار. إغلاق سريع أثناء الانتظار يترك الكاميرا مضاءة بلا إيقاف. |
| رسالة نجاح كاذبة عند الحفظ | `DocumentScannerDialog.tsx → savePdf()` | `toast.success("تم حفظ PDF")` يظهر بلا أي `try/catch` أو تحقق من نجاح `a.click()` — على iOS WKWebView سلوك `<a download>` غير موثوق أصلًا. |
| التقويم الدراسي ينتهي فعليًا 2027 | `src/lib/dailyTools.ts → SCHOOL_EVENTS` | مصفوفة ثابتة تغطي عامًا دراسيًا واحدًا فقط؛ بعد 25 يونيو 2027 تُرحَّل التواريخ تلقائيًا "كتقدير" غير رسمي. |

### عالية
| المشكلة | الملف | الوصف |
|---|---|---|
| رسالة خطأ توجّه لـ"إعدادات المتصفح" | `DocumentScannerDialog.tsx` | لا رابط فعلي لفتح إعدادات نظام iOS؛ المستخدم بلا طريقة عملية لاستعادة الإذن بعد الرفض الأول. |
| لا واجهة استرداد بعد رفض إذن QR | `QRScannerDialog.tsx` | خلافًا لماسح المستندات، لا تصنيف لأنواع الأخطاء ولا زر إعادة محاولة — فقط شريط تنبيه أحمر. |
| صفحة `NotificationDiagnostics.tsx` يتيمة | `src/pages/NotificationDiagnostics.tsx` | 426 سطرًا كاملة الوظيفة، لا `import` ولا `<Route>` في `App.tsx` — غير قابلة للوصول إطلاقًا. |

### متوسطة
| المشكلة | الملف | الوصف |
|---|---|---|
| صفحة 404 بالإنجليزية بالكامل | `src/pages/NotFound.tsx` | لا تستخدم نظام الترجمة `t()` رغم أن التطبيق عربي RTL افتراضيًا. |
| فشل تحميل قائمة السور بلا رسالة | `QuranDialog.tsx` | `.catch(() => {})` صامت — القائمة تبقى فارغة بلا أي تفسير عربي عند فشل الشبكة. |
| `isSaudiHoliday()` غير مكتملة | `dailyTools.ts` | لا تشمل "يوم العلم" رغم وجوده في `NATIONAL_EVENTS` — تعارض مع منطق تأجيل صرف الرواتب. |
| ابتلاع أخطاء `navigator.share` الحقيقية | `DocumentScannerDialog.tsx` | كل خطأ يُعامَل كإلغاء من المستخدم؛ لا تمييز عن فشل حقيقي (مثل `AbortError` تحديدًا). |
| `manualCapture()` يفشل بصمت | `DocumentScannerDialog.tsx` | عند عدم توفر أبعاد الفيديو بعد، الدالة تعود دون أي `toast`/رسالة. |
| الفلاش (Torch) قد لا يعمل عمليًا على iOS | كلا الماسحين | يعتمد على `MediaTrackCapabilities.torch` غير المطبَّق بالكامل في WebKit. |

### كود ميت مؤكَّد (غير مستورد في أي مكان، تحقّق فعلي بالبحث)
~1900 سطر إجمالًا عبر 11 ملف/مكوّن:
`AppointmentsPanel.tsx` (533 سطر، نظام مواعيد بديل كامل) · دالة `inheritance()` داخل `dailyTools.ts` (نسخة مواريث مبسّطة وأقل دقة) · `src/pages/NotificationDiagnostics.tsx` · `PrayerGrid.tsx` · `WeatherAlerts.tsx` · `NetworkSpeedPill.tsx` · `NextAthanBanner.tsx` · `OnboardingPermissions.tsx` · `src/components/NavLink.tsx` · `src/lib/pushNotifications.ts` (145 سطر، تدفّق Web Push كامل استُبدل بالإشعارات الأصلية دون حذف القديم).

---

## 3. الأدوات التي يجب حذفها

### 3.1 كود ميت داخل الأدوات (حذف آمن ومباشر)
- **`AppointmentsPanel.tsx`** — غير مستورد إطلاقًا، ويعتمد على `Notification` الخاصة بالمتصفح (غير موثوقة داخل Capacitor) خلافًا لـ`EventsCalendar.tsx` الفعّالة.
- **دالة `inheritance()`** في `dailyTools.ts` — نسخة مبسّطة غير مستخدَمة، خطر استخدامها بالخطأ مستقبلًا بدل `calculateInheritance()` الفعلية المُختبرة.

> ⚠️ **قرار مطلوب قبل الحذف:** `NextAthanBanner.tsx` و`OnboardingPermissions.tsx` قد يكونان أُبقيا عمدًا لإعادة استخدام قريب (القسم 5 يقترح إحياء `NextAthanBanner` بدل حذفه كشريط "الأذان القادم" في الصفحة الرئيسية).

### 3.2 TikTok و YouTube — خطة حذف آمن (لم تُنفَّذ)

**الاكتشاف الجوهري:** هذه ثلاث ميزات منفصلة منطقيًا وليست ميزة واحدة:

| الميزة | الحالة | الاستقلالية |
|---|---|---|
| تحليل حساب TikTok (`/tiktok`) | فعّالة | مستقلة تمامًا — حذف مباشر وآمن |
| تنزيل فيديو TikTok | فعّالة | داخل `DownloadManager.tsx` المشترك |
| تنزيل فيديو YouTube | **معطّلة أصلًا** (`YOUTUBE_ENABLED=false`) | داخل نفس المكوّن |

> ⚠️ **قرار محوري مطلوب منك أولًا:** `DownloadManager.tsx` **ليس** حصريًا لتيك توك/يوتيوب — نفس المكوّن يشغّل "تنزيل أي رابط عام" ويغذّي سجل الملفات وجدول `download_events` في لوحة الإدارة.
> - **الخيار A — حذف جذري:** حذف المكوّن كاملًا؛ يُفقد معه تنزيل الروابط العامة وسجل الملفات كأثر جانبي.
> - **الخيار B — حذف جراحي (الأنسب لما طُلب حرفيًا):** إبقاء التنزيل العام وسجل الملفات، وإزالة كود/واجهة TikTok وYouTube فقط (إعادة كتابة جزئية لملف واحد).

**خطوات الحذف الآمن المرقّمة (الترتيب مهم لتفادي الكسر):**

*المرحلة 1 — إزالة محلل TikTok (آمنة في كلا الخيارين):*
1. حذف الاستيراد و`<Route path="/tiktok">` من `App.tsx`.
2. حذف عنصر `"tiktok"` من `BottomNav.tsx` (النوع والعنصر معًا).
3. حذف رابط `/tiktok` من `SideMenu.tsx`.
4. حذف فرع `next === "tiktok"` في `Index.tsx`.
5. الآن فقط: حذف `src/pages/TikTok.tsx` و`src/lib/tiktokHistory.ts`.
6. حذف مجلد `supabase/functions/tiktok-profile/` وسطره في `config.toml`.
7. حذف مفاتيح `TIKTOK_ANALYZER_*` من `adsConfig.ts`.
8. تحقق ببناء تجريبي (لا مرجع معلّق لنوع `AdSlotKey`).

*المرحلة 2 — حسب الخيار المُختار:*
9. **خيار A:** حذف `DownloadManager.tsx` بالكامل + استدعائه في `Index.tsx` (وربما تبويب "الوسائط" كليًا).
   **خيار B:** إزالة `TIKTOK_RE`/`YOUTUBE_RE`/منطق الاستخراج/واجهة اختيار المصدر فقط، مع الإبقاء على `startJobFromUrl` وتبويب "الملفات".
10. حذف مفاتيح `tiktokDownload*` و`youtubeDownload*` من `adsConfig.ts`.
11. حذف مجلدات `tiktok-extract`, `tiktok-proxy`, `youtube-extract` وأسطرها من `config.toml`.

*المرحلة 3 — تنظيف نصي:* إعادة صياغة `About.tsx` (يذكر TikTok/YouTube صراحة) ووصف SEO الرئيسي في `Index.tsx`، وتحديث ملاحظة `IOS_BUILD.md` (البند 5.2.3).

*المرحلة 4 — تحقق نهائي:* `npm run build` + بحث شامل `grep -ri "tiktok\|youtube"` للتأكد من عدم بقاء أثر غير مقصود.

**ملاحظة أمان:** جدول `download_events` لا يُحذف في أي من الخيارين — بيانات تاريخية مشتركة مع تنزيلات أخرى. أذونات iOS (`Info.plist`) عامة وغير مرتبطة حصريًا بهاتين الأداتين، فلا حاجة لتعديلها.

---

## 4. الأدوات التي تحتاج إصلاحًا (خصوصًا QR وماسح المستندات)

**الاكتشاف الجوهري:** لا استخدام لأي إضافة Capacitor أصلية في أي من الماسحين (لا Camera، لا Filesystem، لا Share، لا Barcode Scanning) — كل شيء Web API قياسي (`getUserMedia`, `<a download>`, `navigator.share`) يعمل عبر WKWebView.

### ما يعمل فعليًا وبجودة جيدة (لا يحتاج إعادة كتابة)
- **القص (Crop):** تحويل منظوري حقيقي (perspective warp بحل مصفوفة 8×8 يدويًا) — يعمل.
- **التدوير (Rotate):** يعمل فعليًا عبر إعادة حساب زوايا الشكل الرباعي.
- **التحسين (Enhance):** توازن بياض تلقائي + تباين + شحذ (unsharp mask) — منطق مكتوب يدويًا وسليم.
- كشف حواف المستند التلقائي (Sobel gradient + خط انحدار) يعمل لكنه يعتمد على دقة منخفضة (200px) تتأثر بضعف الإضاءة.

### ما يحتاج إصلاحًا فعليًا
1. **إصلاح تسريب الـ stream** — إضافة علم `cancelled`/`isMounted` يُتحقق منه بعد كل `await` في `start()` قبل إسناد الـ stream.
2. **الانتقال إلى `@capacitor/camera` + `@capacitor/filesystem` + `@capacitor/share`** بدل الاعتماد الحصري على Web API الخام — يحل مشاكل الحفظ غير الموثوق والمشاركة والصلاحيات معًا.
3. **فتح إعدادات نظام iOS فعليًا** عند رفض الإذن (عبر Capacitor App plugin) بدل رسالة "إعدادات المتصفح" المضلِّلة.
4. **توحيد معالجة الأخطاء في QR** لتطابق ماسح المستندات (تصنيف حسب `e.name`، زر إعادة محاولة، timeout مع رسالة "لم يُعثر على رمز").
5. **تمييز إلغاء المستخدم عن فشل حقيقي** في `navigator.share` (فحص `error.name === "AbortError"` تحديدًا).
6. **(تحسين اختياري) ربط ماسح المستندات بـ OCR مباشرة** — حاليًا منفصلان تمامًا؛ `OcrTool.tsx` يتطلّب من المستخدم رفع صورة جديدة يدويًا بدل استخدام نتيجة المسح المُحسَّنة مباشرة عبر `ai-ocr`.

### أدوات أخرى تحتاج إصلاحًا (خارج الماسحين)
- **التقويم/الإجازات الدراسية** (`dailyTools.ts`) — يحتاج سياسة تحديث يدوي سنوي واضحة أو مصدرًا حيًا بديلاً قبل يونيو 2027 (تفصيل كامل في القسم 7).
- **`isSaudiHoliday()`** — إضافة يوم العلم لمنطق تأجيل الصرف.
- **`NotFound.tsx`** — ربطها بنظام الترجمة `t()`.
- **`QuranDialog.tsx`** — إضافة رسالة خطأ عربية عند فشل تحميل السور/الآيات.

---

## 5. اقتراح ترتيب جديد لمركز الخدمات

مبني مباشرة على نتائج القسم 1 — تصحيح ما هو غير متّسق فعليًا، وليس إعادة اختراع هوية بصرية جديدة.

### مركز الخدمات
- تجميع الـ33 أداة في فئات كبيرة بدل شبكة مسطّحة واحدة:
  - **الحاسبات الشرعية/المالية**: زكاة، مواريث، عمر، تمويل، سعي.
  - **التقويم والمناسبات**: المحوّل الهجري/الميلادي المُدمَج (انظر القسم 6)، المناسبات الإسلامية والوطنية، الإجازات الرسمية والدراسية.
  - **أدوات عامة**: عملات، وحدات، محوّل الملفات.
- بطاقات أكبر (تقليل عدد ما يُعرض دفعة واحدة)، مع مربع بحث/تصفية أعلى القائمة بدل التمرير الكامل عبر 33 بطاقة.
- شبكة متجاوبة فعليًا حسب عرض الشاشة: عمودان على iPhone SE، ثلاثة أعمدة من عرض معيّن فأكبر (بدل عمودين ثابتين من SE إلى Pro Max).

### الصفحة الرئيسية
- إبقاء بطاقة الصلاة كعنصر أول، مع إحياء `NextAthanBanner.tsx` (كود جاهز غير مستخدم حاليًا) كشريط "الأذان القادم" بدل حذفه.
- اختصارات سريعة محدودة (4–6 أدوات الأكثر استخدامًا) بدل عرض كل شيء دفعة واحدة، مع رابط "عرض الكل" لمركز الخدمات الكامل.

### نظام تصميم موحّد مقترح

| العنصر | المشكلة الحالية | المقترح |
|---|---|---|
| سلّم الطباعة | عشرات قيم `text-[Npx]` الحرّة | 5 مستويات ثابتة: caption / body / subtitle / title / h1 |
| الألوان والتدرّجات | معرَّفة يدويًا في ملفين JS منفصلين | مصدر واحد: `--gradient-*` في `index.css` فقط |
| حاويات الأيقونات | `h-7` إلى `h-20` بلا نظام | 3 مقاسات ثابتة: صغير 32 / متوسط 44 / كبير 56 |
| سهم الرجوع RTL/LTR | 4 أساليب متضاربة، إحداها معكوسة | مكوّن `<BackButton/>` واحد موحّد |
| حالات التحميل/الفراغ | Skeleton جاهز وغير مستخدم | اعتماد Skeleton + مكوّن `EmptyState` موحّد للجميع |

### ما لا يحتاج تغييرًا
الشريط السفلي (safe-area مطبَّق بعناية بالفعل)، دعم `100dvh`، اختيار خط Cairo/Nunito للعربية، ومكتبة الأيقونات lucide-react — سليمة وتستحق البقاء كما هي.

---

## 6. الأدوات التي يمكن دمجها أو اختصارها

| الأداة | الملفات | السبب والإجراء المقترح |
|---|---|---|
| محوّلا الهجري/الميلادي | `EliteTools.tsx → DateConverterTool` و`Calculators.tsx → HijriConverter` | نفس المكتبة (`hijri-converter`) ونفس الوظيفة بالضبط بواجهتين مختلفتين. **الإجراء:** دمج في أداة واحدة ضمن مركز الخدمات، وحذف نسخة `EliteTools`. |
| مدير التنزيلات (عرض مكرر) | `Index.tsx` — تبويب "أدوات" وتبويب "الوسائط" معًا | نفس المكوّن يظهر مرتين في نفس رحلة المستخدم. **الإجراء:** عرضه في مكان واحد فقط. |

---

## 7. البيانات والمناسبات والإجازات التي تحتاج مصادر رسمية موثوقة

| البيانات | الحالة الحالية | التقييم |
|---|---|---|
| **التحويل الهجري الأساسي** | مكتبة `hijri-converter` (جدول ثابت 1741 قيمة، يغطي ~1355–1500هـ / حتى ~2077م) | موثوق لعقود قادمة، لكنه **ليس** اتصالًا رسميًا حيًا بالهيئة العامة للمساحة — تقريب من مكتبة طرف ثالث صغيرة بلا توثيق لتاريخ آخر مطابقة مع التقويم الرسمي المطبوع. |
| **المناسبات الإسلامية** | حساب تلقائي (يوم/شهر هجري ثابت + تحويل) | التطبيق لا يذكر للمستخدم أن بداية رمضان/العيدين الفعلية تعتمد سنويًا على **ترائي هلال رسمي** من المحكمة العليا وقد تختلف يومًا عن الحساب الجدولي. |
| **المناسبات الوطنية** | تاريخ ميلادي ثابت متكرر تلقائيًا | لا تنتهي صلاحيتها (لا تحتاج تحديثًا سنويًا). |
| **الإجازات الرسمية** | دمج القائمتين أعلاه، بلا مصدر إضافي مستقل | تعليق الكود يستشهد بمرجع نظامي عام دون رابط أو نص رسمي مرفق. |
| **⚠️ التقويم/الإجازات الدراسية** | **مصفوفة ثابتة مكتوبة يدويًا لعام دراسي واحد فقط** (١٤٤٨هـ، حتى 2027-06-25) | **حرج.** بعد يونيو 2027 تتحول التواريخ تلقائيًا لتقديرات آلية غير رسمية ("estimate" في تعليق الكود نفسه) ما لم يُحدَّث الملف يدويًا كل عام. |
| **مواعيد صرف الرواتب/حساب المواطن/الضمان الاجتماعي** | منطق تعديل يدوي عند العطلات | لا رابط أو مرجع رسمي مرفق بالكود لقاعدة الحساب. |

### فئات المصادر الرسمية المقترحة للمراجعة اليدوية (وليست مستخدمة حاليًا كاتصال حي)

| نوع البيانات | الجهة الرسمية المناسبة |
|---|---|
| التقويم الهجري / تحويل التاريخ | تقويم أم القرى — الهيئة العامة للمساحة / ترائي الهلال (المحكمة العليا) |
| الإجازات الرسمية | وزارة الموارد البشرية والتنمية الاجتماعية |
| التقويم والإجازات الدراسية | وزارة التعليم (التقويم الدراسي الموحّد) |
| مواعيد الصرف الحكومي | وزارة المالية / برنامج حساب المواطن / الضمان الاجتماعي |

---

## 8. جدول الأولوية

### 🔴 حرج
| البند | القسم |
|---|---|
| تسريب مصدر الكاميرا في ماسح QR والمستندات (Race Condition) | 2, 4 |
| رسالة "تم حفظ PDF" الكاذبة عند فشل الحفظ الفعلي | 2, 4 |
| التقويم الدراسي مكتوب يدويًا وينتهي فعليًا يونيو 2027 | 2, 7 |

### 🟠 مرتفع
| البند | القسم |
|---|---|
| رسالة "إعدادات المتصفح" المضلِّلة في تطبيق iOS أصلي | 2, 4 |
| لا واجهة استرداد بعد رفض إذن كاميرا QR | 2, 4 |
| صفحة `NotificationDiagnostics.tsx` كاملة الوظيفة لكن يتيمة بلا مسار | 2 |
| قرار حذف TikTok/YouTube المعلَّق (خيار A مقابل B) | 3 |

### 🟡 متوسط
| البند | القسم |
|---|---|
| صفحة 404 بالإنجليزية بالكامل | 2, 4 |
| فشل تحميل قائمة السور بلا رسالة خطأ عربية | 2, 4 |
| `isSaudiHoliday()` لا تشمل يوم العلم | 2, 4 |
| كود ميت مؤكَّد (~1900 سطر، 11 ملف) | 2, 3 |
| محوّلا الهجري/الميلادي المكرران | 6 |
| مدير التنزيلات يظهر مرتين | 6 |
| ابتلاع أخطاء `navigator.share` الحقيقية بصمت | 2, 4 |
| الفلاش (Torch) قد لا يعمل عمليًا على iOS | 2, 4 |
| ازدحام مركز الخدمات (33 أداة بعمودين) | 1, 5 |
| عدم استخدام سلّم طباعة/ألوان موحّد | 1, 5 |
| 4 أساليب متضاربة لسهم الرجوع RTL/LTR | 1, 5 |

### 🟢 منخفض
| البند | القسم |
|---|---|
| `manualCapture()` يفشل بصمت بلا رسالة | 2, 4 |
| عدم استخدام مكوّن Skeleton الجاهز في حالات التحميل | 1, 5 |
| لا حالة "لا نتائج" مصمَّمة بعناية | 1, 5 |
| عدم استخدام `React.lazy` للحوارات/الصفحات الثقيلة | 1 |
| ارتفاع ثابت 420px لإطار الطقس على شاشات SE | 1 |
| ربط OCR بماسح المستندات مباشرة (تحسين تجربة استخدام) | 4 |

---

## 9. خطة تنفيذ على مراحل (بدون تعديل أو حذف أي ملف الآن)

> ⏸ **توقّف إلزامي — لا تُنفَّذ أي مرحلة من هذه الخطة قبل مراجعتها واعتمادها صراحةً.**

**المرحلة 0 — قرارات مسبقة مطلوبة قبل أي تنفيذ:**
- خيار A مقابل خيار B لحذف TikTok/YouTube (القسم 3.2).
- مصير `OnboardingPermissions.tsx`/`NextAthanBanner.tsx`: حذف نهائي أم إحياء ضمن إعادة التنظيم (القسم 5)؟
- سياسة تحديث التقويم الدراسي: تحديث يدوي سنوي، أم البحث عن مصدر حي بديل؟

**المرحلة 1 — إصلاحات عاجلة منخفضة المخاطر:**
ترجمة صفحة 404 · إصلاح تسريب stream الكاميرا في كلا الماسحين · معالجة نجاح/فشل الحفظ الحقيقي في ماسح المستندات · تصحيح رسالة "إعدادات المتصفح" · إضافة رسالة خطأ عند فشل تحميل السور.

**المرحلة 2 — تنظيف الأدوات:**
حذف الكود الميت المؤكَّد (القسم 2 و3.1) · دمج محوّلي التاريخ الهجري/الميلادي · تنفيذ خطة حذف TikTok/YouTube حسب الخيار المُعتمَد في المرحلة 0.

**المرحلة 3 — تحديث ماسح QR والمستندات:**
الانتقال إلى إضافات Capacitor أصلية (Camera/Filesystem/Share) · توحيد معالجة الأخطاء وواجهة الاسترداد · اختبار كامل على جهاز iPhone حقيقي قبل الدمج النهائي.

**المرحلة 4 — نظام التصميم وإعادة التنظيم:**
سلّم الطباعة الموحّد · مصدر الألوان/التدرّجات الموحّد · مكوّنا `BackButton`/`EmptyState` · إعادة تصميم مركز الخدمات حسب القسم 5 · مراجعة التجاوب عبر أحجام iPhone المختلفة.

---

*نهاية التقرير — تدقيق قراءة فقط، لم يُعدَّل أو يُحذف أي ملف من المشروع.*
