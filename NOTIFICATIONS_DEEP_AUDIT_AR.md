# تقرير الفحص العميق لإشعارات iOS

## السبب الحقيقي الأوضح الذي وُجد

اختبار "موعد بعد دقيقتين" كان يفشل غالبًا لأن نموذج الموعد كان يختار افتراضيًا "قبل 15 دقيقة". عند إنشاء موعد بعد دقيقتين يصبح وقت الإشعار قبل 13 دقيقة من الآن، لذلك يرفضه iOS باعتباره وقتًا ماضيًا ولا يدخل في `getPending()`.

تم الإصلاح بطريقتين:

1. الخيار الافتراضي للموعد الجديد أصبح "عند الموعد" بدل "قبل 15 دقيقة".
2. إذا اختار المستخدم تذكيرًا يسبق الموعد لكن الوقت المحسوب أصبح ماضيًا، يُجدول الإشعار عند وقت الموعد نفسه بدل إسقاطه.

## مشكلة التزامن

عند فتح التطبيق كانت الصلاة والمواعيد والأذكار تبدأ عمليات الإذن والإلغاء والجدولة في الوقت نفسه من Effects مختلفة. تم إضافة طابور Native مركزي يسلسل كل عمليات `LocalNotifications` بين المجموعات الثلاث، حتى لا تتسابق على iOS.

## إدارة حد iOS

قبل إدخال مجموعة جديدة، تُلغى الإشعارات القديمة الخاصة بنفس المجموعة فقط لتحرير سعة قائمة iOS، ثم تُجدول العناصر الجديدة عنصرًا عنصرًا، وبعدها يتم التحقق من IDs عبر `getPending()`.

## اختلاف iOS وAndroid

أصبح `channelId` يُرسل على Android فقط؛ لا يُمرر إلى iOS لتجنب اختلافات التحقق بين إصدارات الإضافة.

## الأصوات

تم فحص جميع ملفات CAF:

- `astaghfirullah.caf`: 4.20 ثوانٍ
- `athan_fajr.caf`: 29 ثانية
- `athan_ibn_majid.caf`: 29 ثانية
- `athan_madinah.caf`: 29 ثانية
- `athan_makkah.caf`: 29 ثانية
- نغمات المواعيد والأذكار: من 0.6 إلى 2.2 ثانية

كلها أقل من 30 ثانية وموجودة في مشروع Xcode ومسجلة في `project.pbxproj`.

## الملفات المعدلة

- `src/lib/nativeNotify.ts`
- `src/lib/events.ts`
- `src/components/islamic/EventsCalendar.tsx`
- `src/lib/nativeAthan.ts`

## التحقق

- `tsc --noEmit -p tsconfig.json`: نجح بلا أخطاء.
- تعذر `npm ci` في بيئة الفحص بسبب بوابة npm الداخلية التي أعادت 404 لحزمة `zod`، وليس بسبب كود المشروع.
