# تقرير تنظيف وإعادة ترتيب واجهة التطبيق — النخبة الإسلامية
**التاريخ:** 2026-09-14
**الفرع:** `cleanup/ui-declutter` (لم يُدمج بعد مع `main`، ولم يُنفَّذ أي `git push`)
**عدد الـ commits:** 4 (باستثناء الـ baseline) — واحد لكل مرحلة

---

## 1. ما الذي حُذف من الواجهة

### أداتا TikTok وYouTube
لا توجد أداة تنزيل TikTok أو YouTube نشطة في التطبيق — كانتا قد حُذفتا مسبقًا في مراجعة سابقة للمشروع، قبل هذا التنظيف. تم التحقق من هذا وعدم حذف شيء وهمي:
- الحارس الذكي في `DownloadManager.tsx` الذي يعرض رسالة توضيحية عند لصق رابط TikTok/YouTube — **أُبقي عليه** لأنه UX جيد.
- الإحصائيات التاريخية المجمّدة لهما في لوحة تحكم الأدمن (`Admin.tsx`) — **أُبقيت كما هي** بناءً على طلبك (سجل تاريخي).
- النص التسويقي في الصفحة الرئيسية الذي كان يعرض "تحميل الفيديوهات" كميزة رغم أنها لم تعد تعمل — **صُحِّح** إلى "تنزيل الملفات" ليطابق الواقع.

### قسم "التخصيص" الفارغ في الإعدادات
حُذف قسم كامل في `Settings.tsx` كان يحتوي فقط على جملة "قريبًا" بدون أي عنصر تفاعلي فعلي. تم التحقق أولًا عبر بحث شامل في الكود أن لا شيء آخر يشير إليه قبل حذفه.

### الأدوات المتكررة (تفصيل في القسم التالي)
16 أداة كانت تكرارًا حرفيًا لبيانات موجودة بالفعل داخل أداة أخرى — حُذفت من القائمة الرئيسية دون فقدان أي وظيفة.

### ما لم يُحذف عمدًا
- `NotificationDiagnostics.tsx` (426 سطر، كود ميت تقنيًا لأنه غير مربوط بأي مسار) — **تُرك كما هو** لأنه كود إشعارات، والتزامًا الحرفي بطلبك بعدم لمس الإشعارات في هذه المرحلة. سيُعاد تقييمه عند بدء مرحلة الإشعارات.

---

## 2. ما الذي أُعيد ترتيبه

### دمج الأدوات المكررة (43 أداة ← 27 أداة في قسم "مركز الخدمات اليومية")
اكتُشف أن 16 أداة من أصل 27 في "مركز الخدمات اليومية" كانت تعرض بالضبط نفس البيانات المعروضة بشكل مجمّع في أداة أخرى:

| الفئة | الأداة الجامعة (أُبقيت) | الأدوات الفردية المحذوفة (16) |
|---|---|---|
| الرواتب/الصرف | كل مواعيد الصرف | راتب حكومي، راتب قطاع خاص، حساب المواطن، الضمان الاجتماعي، راتب التقاعد |
| التعليم | التقويم الدراسي | بداية الدراسة، إجازة الخريف، نهاية الفصل الأول، إجازة منتصف العام، نهاية العام، الإجازة الصيفية |
| المناسبات الإسلامية | كل المناسبات الإسلامية | رمضان، عيد الفطر، عيد الأضحى، يوم عرفة، عاشوراء، رأس السنة الهجرية |

**لم تُفقد أي وظيفة**: أضفت إمكانية الضغط على أي عنصر داخل الأداة الجامعة لعرض نفس التفاصيل الكاملة (ساعة/دقيقة/ثانية + التاريخ الهجري والميلادي) التي كانت الأداة المنفصلة تعرضها — تم اختباره فعليًا ويعمل (لقطة شاشة تحقّق أدناه). كلمات البحث الخاصة بالأدوات المحذوفة (مثل "رمضان"، "راتب حكومي") أُضيفت إلى الأداة الجامعة، فالبحث لا يزال يجدها.

**أُبقي كما هو دون تغيير** (بناءً على مراجعتك):
- "التقاعد" و"عداد لأي تاريخ" — أداتان مختلفتان فعليًا، ليستا تكرارًا.
- "اليوم الوطني" و"يوم التأسيس" — عنصران فقط، فائدة الدمج أقل.

### الصفحة الرئيسية وقسم المناسبات
تحققت من أن "مركز الخدمات اليومية" (قسم المناسبات) يظهر مباشرة بعد "أدواتي" في تبويب الأدوات — أي بضغطة واحدة من أي مكان في التطبيق (شريط التنقل السفلي) ثم تمرير بسيط. لم يكن هناك حاجة لإعادة هيكلة إضافية.

---

## 3. ما تم إصلاحه في ماسح المستندات وQR

### ماسح المستندات (`DocumentScannerDialog.tsx`)
| # | المشكلة | الإصلاح |
|---|---|---|
| 1 | **(الأهم)** إعادة فتح الماسح بعد تركه في شاشة "المعرض/المراجعة" قد تترك الكاميرا شغّالة لكن غير معروضة (شاشة سوداء صامتة) | أُضيف تحقق دفاعي يُعيد ربط البث الحي بعنصر الفيديو فور ظهوره، بدل الاعتماد على توقيت غير مضمون |
| 2 | فشل تحميل صورة كان يُعلّق الواجهة على "جارٍ المعالجة…" إلى الأبد بدون أي رسالة | أُضيف معالج خطأ + رسالة تنبيه واضحة في كل نقطة استخدام |
| 3 | إعدادات الكاميرا تطلب أبعادًا متعارضة (عرضية 16:9 مع طلب نسبة عمودية 3:4) | صُححت لتطابق النية الفعلية (عمودية) |
| 4 | إطار اكتشاف المستند يُرسم بلون أسود بدل اللون المميز للتطبيق (Canvas لا يفهم `var()`) | يُحسب اللون الفعلي مرة واحدة ويُستخدم مباشرة |
| 5 | زر "السماح بالكاميرا" عند رفض الإذن لا يفتح شيئًا فعليًا (iOS لا يعيد طلب الإذن بعد الرفض) | يظهر الآن زر "فتح الإعدادات" يفتح إعدادات iOS مباشرة عند الرفض تحديدًا |
| 6 | الضغط على زر الالتقاط قبل جاهزية الكاميرا لا يفعل شيئًا بصمت | يظهر الآن تنبيه "الكاميرا غير جاهزة. حاول مرة أخرى" |

### أداة QR (`QRScannerDialog.tsx`)
| # | المشكلة | الإصلاح |
|---|---|---|
| 1 | **(الأهم)** رمز QR بمحتوى **فارغ** كان يُعامَل كـ"لا نتيجة" — تبقى الكاميرا متوقفة والشاشة عالقة بدون أي زر لإعادة المحاولة | تتبع حالة "تم المسح" بشكل منفصل عن نص النتيجة، مع رسالة "(رمز QR فارغ)" عند الحاجة |
| 2 | بيئة بدون دعم كاميرا كانت تُظهر رسالة خطأ تقنية بالإنجليزية غير مترجمة | أُضيف نفس التحقق المسبق المستخدم في ماسح المستندات |
| 3 | نفس مشكلة "فتح الإعدادات" عند رفض الإذن | نفس الإصلاح المطبَّق على ماسح المستندات |

**التحقق الفعلي**: شغّلت التطبيق فعليًا (وليس فقط قراءة الكود) وتأكدت أن كلا المسارين (حالة "لا توجد كاميرا" على هذا الجهاز، بما أن بيئة الاختبار الحالية بدون كاميرا فعلية) يعرضان الرسالة والزر الصحيحين بدون أي خطأ في الـ console — راجع لقطات الشاشة في قسم 4.

### خارج نطاق هذا الإصلاح (فجوات وظيفية وليست أخطاء — لتقرر لاحقًا)
- لا يوجد مولّد QR (الأداة قراءة/مسح فقط).
- لا يوجد مسح QR من صورة مرفوعة (كاميرا حية فقط).
- روابط `tel:`, `mailto:`, `WIFI:`, بطاقات الاتصال (vCard) تُعرض كنص عادي بدون إجراء خاص (اتصال/إضافة جهة اتصال/شبكة واي فاي).

---

## 4. نتائج الفحوصات

| الفحص | النتيجة |
|---|---|
| **TypeScript** (`npx tsc --noEmit`) | ✅ **0 أخطاء** |
| **ESLint** (`npm run lint`) | ⚠️ 92 خطأ + 38 تحذير — **جميعها موجودة مسبقًا قبل هذا التنظيف**، تحققتُ سطرًا بسطر أن كل خطأ في الملفات التي عدّلتها (`DocumentScannerDialog.tsx`, `QRScannerDialog.tsx`, `Counters.tsx`, `ServicesHub.tsx`) يقع في كود لم ألمسه إطلاقًا. **لم يُدخل هذا التنظيف أي خطأ أو تحذير جديد.** بقية الأخطاء منتشرة في ملفات أخرى غير مرتبطة بهذه المهمة (`nativeAthan.ts`, `haptics.ts`, `weather.ts`, `Admin.tsx`, وغيرها) — أغلبها من نوع `no-explicit-any` و`no-empty` وهي ديون تقنية قديمة، ليست جزءًا من هذا التنظيف. |
| **Build** (`npm run build`) | ✅ نجح، أُنتج `dist/` بنجاح (43 ثانية) — التحذيرات الظاهرة (حجم بعض الحزم) موجودة مسبقًا وغير متعلقة بهذا التغيير |
| **Vitest** (`npm run test`) | ✅ 5/5 اختبارات ناجحة (لا توجد اختبارات آلية تغطي الملفات المعدَّلة تحديدًا) |
| **زيارة كل المسارات (29 مسارًا)** فعليًا في متصفح حقيقي (Playwright) | ✅ **0 أخطاء console في كل مسار** — القائمة الكاملة: `/, /tools, /calendar, /athkar, /qibla, /quran, /tasbeeh, /translate, /weather, /notifications, /qr-scanner, /document-scanner, /media, /mushaf, /ai, /ai/background-remover, /ai/image-enhancer, /ai/ocr, /privacy, /terms, /contact, /about, /cookies, /disclaimer, /faq, /saudi-jobs, /government-jobs, /sitemap, /settings` |
| **اختبار تفاعلي فعلي** (ليس فقط تصفح) | ✅ فتح "كل المناسبات الإسلامية" ← الضغط على "رمضان" ← ظهور التفاصيل الكاملة (تحقق بلقطة شاشة). ✅ فتح ماسح المستندات وQR في بيئة بدون كاميرا ← ظهور رسالة "لا توجد كاميرا" الصحيحة بدون أي خطأ. ✅ صفحة الإعدادات بعد حذف قسم "التخصيص" — تباعد نظيف بدون أي أثر متبقٍ (تحقق بلقطة شاشة). |

**ملاحظة بيئية غير متعلقة بهذا التنظيف**: عند تحميل أي صفحة، يظهر خطأ CORS واحد من استدعاء `ipapi.co` (خدمة تحديد الموقع الجغرافي عبر IP) — هذا بسبب قيود الشبكة في بيئة الاختبار هذه فقط (sandbox)، وليس خطأ في الكود؛ يعمل عادةً بشكل طبيعي في تطبيق iOS الفعلي أو نشر حقيقي.

---

## 5. مشاكل متبقية / نقاط تحتاج قرارك قبل مرحلة الإشعارات

1. **`NotificationDiagnostics.tsx`** — صفحة تشخيص إشعارات كاملة (426 سطر) غير مربوطة بأي مسار حاليًا. لم تُلمس في هذه المرحلة. عند بدء مرحلة الإشعارات، يجب تقرير: حذفها نهائيًا، أم ربطها بمسار مخفي (مثلاً للتشخيص الداخلي فقط)؟
2. **اليوم الوطني ويوم التأسيس** — تُركا كأداتين منفصلتين عن "كل المناسبات الوطنية" كما طلبت. إن أردت توحيدهما لاحقًا بنفس أسلوب الدمج المطبَّق على البقية، هذا ممكن بسهولة.
3. **فجوات وظيفية في أداة QR** (غير أخطاء): لا مولّد QR، لا مسح من صورة مرفوعة، لا معالجة خاصة لروابط `tel:`/`WIFI:`/جهات الاتصال — تحتاج قرارك إن كانت مطلوبة مستقبلًا.
4. **ديون ESLint القديمة (92 خطأ)** غير متعلقة بهذا التنظيف إطلاقًا — لم أقترب منها لأنها تمس ملفات كثيرة خارج نطاق هذه المهمة (بعضها إشعارات: `nativeAthan.ts`). يمكن معالجتها في مهمة منفصلة إن أردت.
5. **الاختبار الفعلي على كاميرا حقيقية**: كل إصلاحات ماسح المستندات وQR تحققتُ من صحتها منطقيًا وفي بيئة بدون كاميرا (حالة الخطأ)، لكن السيناريو الأساسي (كاميرا حقيقية تعمل، مسح فعلي لمستند/QR حقيقي) يحتاج اختبارًا على جهاز iPhone حقيقي — لا يمكن التحقق منه في هذه البيئة.

---

## الخطوة التالية
كل التغييرات على فرع `cleanup/ui-declutter` منفصلة تمامًا عن `main`، بأربعة commits واضحة (مرحلة لكل commit) قابلة للمراجعة أو التراجع بسهولة. لم يُنفَّذ أي `git push`. بانتظار موافقتك على الدمج مع `main`، أو أي تعديلات إضافية تريدها قبل ذلك.
