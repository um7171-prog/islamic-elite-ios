# فحص نسخة iOS / App Store — 2026-09-02

هذه النسخة **ليست إعلاناً بأن الإشعارات نجحت على الجهاز بعد**. تم إصلاح المسارات البرمجية والمشكلات القابلة للفحص من المصدر، ويبقى اختبار Build فعلي ثم اختبار وصول الإشعار على iPhone.

## أولوية 1: الإشعارات

- تسجيل `NativeNotificationPlugin` داخل `MainViewController` باستخدام `override open func capacitorDidLoad()`.
- إزالة امتلاك `AppDelegate` اليدوي لـ `UNUserNotificationCenter.delegate` حتى يبقى Capacitor NotificationRouter هو المالك الوحيد لعرض/توجيه Local Notifications وPush Notifications.
- الإبقاء على `presentationOptions = badge, sound, banner, list` في Capacitor LocalNotifications.
- تقوية تحويل أرقام `id` و`atMs` القادمة من JavaScript داخل Swift (`NSNumber`/Int/Double/String) لمنع إسقاط عناصر بصمت بسبب اختلاف نوع الجسر.
- إعادة بناء مجموعة الإشعارات أصبحت تحذف **العناصر القديمة فقط** بدل مسح المجموعة كلها قبل محاولة الجدولة الجديدة.
- عند تعطيل كل إشعارات مجموعة ما، تمر القائمة الفارغة إلى Swift لتنظيف الإشعارات القديمة فعلاً.
- رفع تغطية تنبيهات الصلاة إلى 3 أيام بحد أقصى 42 إشعاراً للصلاة، مع إبقاء إجمالي الحدود النظرية تحت حد iOS البالغ 64: صلاة 42 + مواعيد 12 + أذكار 6 + اختبار 1 = 61.
- إضافة مجموعة اختبار مستقلة `900000–900099` لا يمكن لإعادة جدولة الصلاة/المواعيد/الأذكار حذفها.
- استبدال صفحة التشخيص المؤقتة بزر مستخدم واضح داخل الإعدادات: «اختبار بعد 12 ثانية»، ويستخدم **نفس مسار الإنتاج NativeNotification**.
- أصوات `.caf` الموجودة تم فحصها: كلها أقل من 30 ثانية وموجودة ضمن Copy Bundle Resources.
- لا توجد علامات Git conflict (`<<<<<<<`, `=======`, `>>>>>>>`) في المشروع.
- ملفات Swift المعدلة اجتازت `swiftc -parse` بدون خطأ نحوي.

## إشعارات الإدارة (Push)

- أصبحت اختيارية وبموافقة صريحة من المستخدم من داخل الإعدادات.
- افتراضياً لا يتم تسجيل الجهاز لإشعارات الإدارة حتى يفعّلها المستخدم.
- عند تعطيلها يتم تعليم التوكن المخزن `enabled=false` في قاعدة البيانات إن كان موجوداً.

## خصوصية / Apple

- أخفيت من واجهة iOS المثبتة: TikTok Analyzer، Media Downloader، وروابطهما في القائمة والشريط السفلي وصفحات المعلومات. نسخة الويب تحتفظ بها.
- `/tiktok` و`/media` يعيدان التوجيه داخل iOS بدلاً من فتح الأدوات المحظورة.
- أوقفت Google AdSense داخل تطبيق iOS، مع تحميله ديناميكياً على الويب فقط.
- أوقفت تحليلات الزيارة وطلب الموقع التقريبي عبر IP داخل تطبيق iOS، وبقي سلوك الويب منفصلاً.
- حذفت من `Info.plist` صلاحيات غير لازمة للنسخة الحالية: الميكروفون، الموقع الدائم، وTracking.
- أبقيت فقط صلاحيات الكاميرا، الصور، الموقع أثناء الاستخدام، والحركة/البوصلة.
- لم يعد التطبيق يطلب الموقع تلقائياً عند أول تشغيل؛ يتم طلبه عند استخدام ميزة تحتاجه مثل القبلة. إذن الإشعارات يطلبه مسار الجدولة الأصلي.
- OCR والترجمة في iOS يعرضان موافقة صريحة قبل أول إرسال للنص/الصورة إلى Google Gemini عبر Lovable AI Gateway.
- تم تحديث سياسة الخصوصية داخل التطبيق بهذا الإفصاح.

## فحوص تمت محلياً

- TypeScript/TSX: فحص نحوي لكل `src` وSupabase Functions — 0 أخطاء نحوية.
- Swift: `swiftc -parse` لملفات AppDelegate/MainViewController/NativeNotificationPlugin — ناجح.
- Xcode project: `MainViewController.swift` و`NativeNotificationPlugin.swift` موجودان في Compile Sources.
- Storyboard: يستخدم `MainViewController` فعلياً.
- ملفات أصوات الإشعارات موجودة في Copy Bundle Resources.

## ما يجب عدم ادعائه قبل الاختبار

1. لا نعتبر الإشعارات محلولة نهائياً حتى ينجح Build في Codemagic ثم يصل اختبار الـ12 ثانية فعلياً على iPhone والشاشة مقفلة.
2. لا يمكن إثبات حقوق تلاوات القرآن أو شروط Windy/Open-Meteo/JSearch من الكود وحده؛ يلزم مراجعة شروط/تراخيص المزودين قبل الإرسال للمتجر.
