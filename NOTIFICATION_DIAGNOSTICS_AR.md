# أداة فحص إشعارات iPhone

تمت إضافة صفحة داخلية مؤقتة على المسار:

`/notifications-diagnostics`

تعرض الصفحة:
- المنصة.
- صلاحية الإشعارات.
- نجاح استدعاء الجدولة.
- وجود إشعار الاختبار داخل `getPending()`.
- عدد إشعارات الصلاة والمواعيد والأذكار المعلقة.
- وقت اختبار بعد دقيقة.
- آخر خطأ فعلي.

زر **اختبار بعد دقيقة** يستخدم المعرّف `900001` وصوت النظام الافتراضي، ولا يلغي إشعارات الصلاة أو المواعيد أو الأذكار.

كما تم إصلاح تكرار الحقل `eventsPending` في `src/lib/nativeNotify.ts`.
